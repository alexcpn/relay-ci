linters-settings:
  errcheck:
    exclude-functions:
      - (io.Closer).Close
      - (*net/http.Response).Body.Close

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
